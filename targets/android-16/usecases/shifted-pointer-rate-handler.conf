USECASE_ID=shifted-pointer-rate-handler
USECASE_DESCRIPTION='Restore the compiled-out sampling_rate handler for the Android 16 pointer layout shifted by an inserted private-state block'

SHIFTED_RATE_FUNCTION='_ZN3qti5audio4core18MiStreamOutPrimary19setVendorParametersERKNSt3__16vectorIN4aidl7android8hardware5audio4core15VendorParameterENS3_9allocatorISA_EEEEb'
SHIFTED_STANDBY_SYMBOL='_ZN3qti5audio4core18MiStreamOutPrimary7standbyEv'
SHIFTED_CONFIGURE_FUNCTION='_ZN3qti5audio4core16StreamOutPrimary9configureEv'

# The end of setVendorParameters() is structurally shared, but the private
# state inserted before these fields moves the app-state word from +0x810 to
# +0x838.  The actual hook remains the displaced `mov x0, x23`.
SHIFTED_RATE_CONTEXT='a83a08b933008052770000b4????????????????f3000034'
SHIFTED_RATE_SITE_DELTA=12
SHIFTED_RATE_STOCK='e00317aa'

# Platform* +0x780, AudioPortConfig* +0x788 and cached PAL attributes +0x810.
SHIFTED_PLATFORM_CONFIG_LAYOUT='a0c243f9a1c643f9e8430291e2031f2a'
SHIFTED_CACHED_ATTR_LAYOUT='a00a44f9028505ada80a04f9600000b4'

# configure() locks the mutex at +0x7b8 and consumes the AudioPortConfig
# sample-rate optional at +0x8/+0xc.
SHIFTED_CONFIG_MUTEX_LAYOUT='f30300aa00e01e91481740f9????????????????68c643f9'
SHIFTED_SAMPLE_RATE_LAYOUT='68c643f909314039????????090980b9'

# The base standby PAL handle and Xiaomi usecase fields move in lockstep.
SHIFTED_PAL_HANDLE_LAYOUT='08e441f9e80600b4610240f9'
SHIFTED_USECASE_TAG_LAYOUT='74f24679e003142a????????80000037'

# This is still the linker-owned zero range after makeIModuleDefaultQti().
SHIFTED_CAVE_ANCHOR='3f2303d5fd7bbfa9fd030091????????????????00f140f9fd7bc1a8bf2303d5c0035fd6'
SHIFTED_CAVE_ANCHOR_SIZE=36
SHIFTED_CAVE_REQUIRED_SIZE=256
SHIFTED_CAVE_ALIGNMENT=4
SHIFTED_CAVE_OWNER_CALL_DELTA=12
SHIFTED_CAVE_OWNER_PLT='_Z21makeIModuleDefaultQtiv'

SHIFTED_STR_PARMS_GET_STR_PLT=str_parms_get_str
SHIFTED_ATOI_PLT=atoi
SHIFTED_MUTEX_LOCK_PLT='_ZNSt3__15mutex4lockEv'
SHIFTED_MUTEX_UNLOCK_PLT='_ZNSt3__15mutex6unlockEv'
